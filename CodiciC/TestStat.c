#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

// prototipi delle funzioni
int main();
// inizializzazione delle variabili globali

void initialize_global() {
}

//-----------Implementazione funzioni-----------
int main ( ){
	initialize_global();
	// Dichiarazione variabili
	int a;
	char*  sa;
	int x;
	// Assegnazione variabili
	a = 2;
	x = 2;
	sa = "CIAO";

	// Inizio Statement
	if((x > a)){
		// Dichiarazione variabili
		int x3;
		// Assegnazione variabili
		x3 = 2;

		// Inizio Statement
		return a;

	}
	while(( !(((((x / 1) < 2.2) && ((x + (1 / 2)) < 31)) || (((x * 2) / 2) > 2)) && ( !(((x - 2) / 3) < 2))))){


		// Inizio Statement
		printf("Inserisci\n");
		fflush(stdout);
		scanf("%d", &x);
		printf("%s %d\n","Stampaaaaaa ", x);
		fflush(stdout);

	}
	for( int sa = 1; sa <= 4;sa++){



	}

}
