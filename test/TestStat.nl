

start: def main () : integer {

    integer a << 2;
    integer x << 2;
    string saa << "CIAO";

    if x > a then {
        integer x3 << 2;
        return a;
    }

    while not x / 1 < 2.2 and x + 1 / 2 < 31 or x * 2 / 2 > 2 and not x-2 /3 < 2 loop {
        x <-- "Inserisci";
        ("Stampaaaaaa ", x) -->!;
    }

    for saa << 1 to 4 loop {

    }


}
